---
context: phase
phase: 02-full-venue-heatmap
task: 0
total_tasks: 0
status: ready_to_plan
last_updated: 2026-05-08T17:29:08Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified this session. Proceed normally._

<current_state>
Phase 2 context has been gathered and committed. CONTEXT.md and DISCUSSION-LOG.md are in `.planning/phases/02-full-venue-heatmap/`. No PLAN.md exists yet — next step is to plan the phase.

KEY ARCHITECTURAL CHANGE vs. original spec: VIZ-01 is **not** an overlay heatmap. It is a **dual-panel side-by-side layout** — left panel = live simulation (agents), right panel = sniffer KDE heatmap. This changes the window from 600×420 (Phase 1) to 1200×520. The original ROADMAP.md description ("sovrapposta alla mappa con alpha blending") is superseded by 02-CONTEXT.md.
</current_state>

<completed_work>

This session:
- Phase 2 discuss-phase completed (all 4 gray areas discussed)
- 02-CONTEXT.md written and committed (d568ff3)
- 02-DISCUSSION-LOG.md written and committed (d568ff3)

Prior sessions:
- Phase 1 fully complete: 4/4 plans executed, 12/12 verification passed, human-approved (47b361b)
</completed_work>

<remaining_work>

- [ ] Plan Phase 2 (`/gsd-plan-phase 2`)
- [ ] Execute Phase 2 plans (`/gsd-execute-phase 2`)
</remaining_work>

<decisions_made>

All Phase 2 decisions are in `02-CONTEXT.md`. Key ones:

- **Dual-panel layout**: left=agents, right=KDE heatmap (viridis). Window 1200×520.
- **Control panel**: horizontal bar at bottom spanning full width. Map [S][M][L], Behavior [W][G][C], sliders for n_people/σ_error/σ_kernel, Reset button.
- **Tab bar**: between map panels and control panel (bottom of map area).
- **Map scaling**: all maps render to fixed 600×400 canvas (scale down, never resize window).
- **Goal-oriented**: random POI loop, dwell 1-3s, repeat. No exit.
- **Social**: K=10 centroid attraction + jitter.
- **σ sliders**: live update. Map/n_people: apply on Reset only.
- **Phase 2 adds Reset button** (Phase 3 adds Start/Pause on top).
- **Initial heatmap**: flat low-intensity viridis from frame 0.
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)

1. `.planning/phases/02-full-venue-heatmap/02-CONTEXT.md` — All Phase 2 decisions. MUST read before planning.
2. `.planning/REQUIREMENTS.md` §Phase 2 — MAP-01–MAP-05, SIM-03, SIM-04, CTR-01, VIZ-01, VIZ-05
3. `.planning/ROADMAP.md` §Phase 2 — Success criteria (5 items); note that criterion #4 ("smooth colour heatmap blended over the map") is now a side-by-side panel, not an overlay
4. `crowd_mvp/simulation.py` — Simulation class to extend for behavior + reset support
5. `crowd_mvp/viz/heatmap.py` — Stub to implement: `build_heatmap_surface(sniffer_positions, sniffer_counts, map_size, sigma_kernel)`

## Critical Anti-Patterns (do NOT repeat these)

None identified this session.

## Infrastructure State

- Single-process Pygame app — no servers or background processes
- Entry point: `python crowd_mvp/main.py`
- All dependencies: Python 3.10+, pygame, numpy, scipy, matplotlib (for colormap only — no plt.show())

<context>
This session we gathered the Phase 2 context through a full 4-area discussion. The most important outcome is the layout change: instead of a 600×420 window with a heatmap overlay, Phase 2 delivers a 1200×520 dual-panel view that shows simulation vs. heatmap side by side — the "realtà vs stima" story is immediately visible without switching tabs. This is the pitch's visual centrepiece.

The planner should treat 02-CONTEXT.md as the authoritative spec for what to build. ROADMAP.md success criteria are directionally correct but the layout specifics are superseded by the context decisions.
</context>

<next_action>
Start with: `/gsd-plan-phase 2` — context is ready, planner can proceed immediately.
</next_action>
